apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: heritageflix-zuiderzeemuseum-locaties
  namespace: nde
spec:
  serviceAccountName: nde
  interval: 30m
  chart:
    spec:
      chart: helm/nde-app
      sourceRef:
        kind: GitRepository
        name: nde
  values:
    containers:
      - name: app
        image:
          repository: ghcr.io/netwerk-digitaal-erfgoed/heritageflix-zuiderzeemuseum-locaties
          tag: latest # {"$imagepolicy": "nde:heritageflix-zuiderzeemuseum-locaties:tag"}
        port: 3000
        flux:
          enabled: true

    ingresses:
      - hosts:
          - heritageflix-zuiderzeemuseum-locaties.demo.netwerkdigitaalerfgoed.nl
