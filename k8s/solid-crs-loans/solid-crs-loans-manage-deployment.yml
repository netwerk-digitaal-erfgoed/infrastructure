apiVersion: apps/v1
kind: Deployment
metadata:
  name: solid-crs-loans-manage
spec:
  replicas: 2
  selector:
    matchLabels:
      app: solid-crs-loans-manage
  template:
    metadata:
      labels:
        app: solid-crs-loans-manage
    spec:
      containers:
        - name: app
          image: ghcr.io/netwerk-digitaal-erfgoed/solid-crs-loans-manage
          ports:
            - containerPort: 80
      imagePullSecrets:
        - name: ghcr
