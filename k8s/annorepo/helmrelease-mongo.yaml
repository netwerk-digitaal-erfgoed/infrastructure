apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: annorepo-mongo
  namespace: nde
spec:
  serviceAccountName: nde
  interval: 30m
  chart:
    spec:
      chart: helm/nde-app
      reconcileStrategy: Revision
      sourceRef:
        kind: GitRepository
        name: nde
  values:
    workload:
      type: statefulset

    containers:
      - name: app
        image:
          repository: mongo
          tag: "8.0.9"
        flux:
          policy:
            type: semver
            range: "~8.0"
        port: 27017
        volumeMounts:
          - mountPath: /data/db
            name: data

    service:
      port: 27017

    persistentVolumes:
      - name: data
        size: 8Gi
