apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: network-of-terms-demo
  namespace: nde
spec:
  serviceAccountName: nde
  interval: 30m
  chart:
    spec:
      chart: helm/nde-app
      reconcileStrategy: Revision
      sourceRef:
        kind: GitRepository
        name: nde
  values:
    workload:
      replicas: 2
    containers:
      - name: app
        image:
          repository: ghcr.io/netwerk-digitaal-erfgoed/network-of-terms-demo
          tag: 20260226204006-ccc1e09 # {"$imagepolicy": "nde:network-of-terms-demo:tag"}
        port: 80
    ingresses:
      - hosts:
          - termennetwerk.netwerkdigitaalerfgoed.nl
